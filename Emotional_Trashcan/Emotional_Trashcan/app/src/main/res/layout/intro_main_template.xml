<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/intro_main_template"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#00000000"
    android:gravity="bottom"
    android:orientation="vertical">

    <include
        android:id="@+id/intro_main_top"
        layout="@layout/top_toolbar"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/intro_main_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/multi_coach_marks_main_caption"
        android:textColor="#ffffff"
        android:textSize="20sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/intro_trash_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/intro_main_bottom_toolbar"
        android:layout_marginBottom="50dp"
        android:background="@drawable/arrow_down2" />

    <ImageButton
        android:id="@+id/intro_main_btngotit"
        android:layout_width="58dp"
        android:layout_height="52dp"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/intro_main_bottom_toolbar"
        android:background="@drawable/heartbutton" />
    <include layout="@layout/bottom_toolbar"
        android:id="@+id/intro_main_bottom_toolbar"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:visibility="invisible"
        />
</RelativeLayout>